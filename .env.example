# URL de l'API Backend Spring Boot
# En développement, le proxy Vite redirige déjà /api vers localhost:8080
# Cette variable sert pour un déploiement où le frontend et le backend sont sur des domaines différents
VITE_API_URL=/api/v1
